apiVersion: v1
kind: Service
metadata:
  name: stockloom-backend-active
  namespace: stockloom
spec:
  selector:
    app: stockloom-backend
  ports:
    - port: 8000
      targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: stockloom-backend-preview
  namespace: stockloom
spec:
  selector:
    app: stockloom-backend
  ports:
    - port: 8000
      targetPort: 8000
