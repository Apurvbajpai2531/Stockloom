apiVersion: v1
kind: Service

metadata:
  name: stockloom-backend-service
  namespace: stockloom

spec:
  selector:
    app: stockloom-backend

  type: NodePort

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30008