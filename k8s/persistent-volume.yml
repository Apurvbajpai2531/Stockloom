apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv
  labels:
    app: stockloom-postgres
spec:
  capacity:
    storage: 500Mi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: /mnt/stockloom-postgres
