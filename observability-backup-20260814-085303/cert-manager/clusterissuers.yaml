apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned-issuer
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: stockloom-root-ca
  namespace: cert-manager
spec:
  isCA: true
  commonName: stockloom-root-ca
  secretName: stockloom-root-ca-secret
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
    group: cert-manager.io
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: stockloom-ca-issuer
spec:
  ca:
    secretName: stockloom-root-ca-secret
